{
  "name": "mahitek-3d-lab-mx",
  "version": "1.0.0",
  "description": "Landing page oficial de Mahitek 3D Lab - Impresión 3D funcional en PETG",
  "main": "assets/js/app.js",
  "scripts": {
    "dev": "live-server --port=8080 --no-browser --watch=. --ignore=node_modules",
    "dev:open": "live-server --port=8080 --watch=. --ignore=node_modules",
    "start": "npm run dev",
    "serve": "python -m http.server 8080 || npx serve -l 8080",
    "serve:lighthouse": "serve -l 8080 --no-clipboard --cors",
    "format:js": "prettier --write assets/js/**/*.js",
    "format:css": "prettier --write assets/css/**/*.css",
    "format:html": "prettier --write *.html",
    "format": "prettier --write \"assets/js/**/*.js\" \"assets/css/**/*.css\" \"*.html\"",
    "check:format": "prettier --check \"assets/js/**/*.js\" \"assets/css/**/*.css\" \"*.html\"",
    "validate:html": "node scripts/validate-html.js",
    "validate": "npm run check:format && npm run validate:html",
    "lighthouse": "node scripts/run-lighthouse.js",
    "lighthouse:badges": "npm run lighthouse && node scripts/generar-lighthouse-badges.js",
    "watch:css": "echo 'CSS directo - no requiere compilación'",
    "build:css:min": "cleancss -o assets/css/styles.min.css assets/css/styles.css && cleancss -o assets/css/modules/animations.min.css assets/css/modules/animations.css && cleancss -o assets/css/promo.min.css assets/css/promo.css",
    "build:js:min": "terser assets/js/app.js --module --compress --mangle -o assets/js/app.min.js && terser assets/js/boot.js --module --compress --mangle -o assets/js/boot.min.js && terser assets/js/main.js --module --compress --mangle -o assets/js/main.min.js",
    "build:assets": "npm run build:css:min && npm run build:js:min",
    "build": "npm run format && npm run build:assets",
    "deploy": "git add . && git commit -m 'chore: actualizar sitio' && git push origin main",
    "version:patch": "npm version patch -m 'chore(release): v%s'",
    "version:minor": "npm version minor -m 'feat(release): v%s'",
    "version:major": "npm version major -m 'BREAKING CHANGE: v%s'",
    "preinstall": "echo '📦 Instalando dependencias de desarrollo...'",
    "postinstall": "echo '✅ Instalación completa. Ejecuta: npm run dev'"
  },
  "keywords": [
    "impresion-3d",
    "petg",
    "fabricacion-digital",
    "pachuca",
    "mexico"
  ],
  "author": "Mahitek 3D Lab",
  "license": "MIT",
  "devDependencies": {
    "@lhci/cli": "^0.13.0",
    "clean-css-cli": "^5.6.3",
    "csso": "^5.0.5",
    "http-server": "^14.1.1",
    "jsdom": "^24.1.0",
    "live-server": "^1.2.2",
    "prettier": "^3.3.0",
    "puppeteer": "^24.26.1",
    "serve": "^14.2.5",
    "terser": "^5.44.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%",
    "not dead",
    "not IE 11"
  ]
}
