{
  "name": "mahitek-3d-lab-mx",
  "version": "1.0.0",
  "description": "Landing page oficial de Mahitek 3D Lab - Impresión 3D funcional en PETG",
  "main": "assets/js/app.js",
  "scripts": {
    "dev": "web-dev-server --port=8080 --root-dir . --watch",
    "dev:open": "web-dev-server --port=8080 --root-dir . --watch --open",
    "start": "npm run dev",
    "serve": "python -m http.server 8080 || npx serve -l 8080",
    "format:js": "prettier --write assets/js/**/*.js",
    "format:css": "prettier --write assets/css/**/*.css",
    "format:html": "prettier --write \"*.html\" \"qr/**/*.html\"",
    "format": "prettier --write \"assets/js/**/*.js\" \"assets/css/**/*.css\" \"*.html\" \"qr/**/*.html\"",
    "check:format": "prettier --check \"assets/js/**/*.js\" \"assets/css/**/*.css\" \"*.html\" \"qr/**/*.html\"",
    "monitor": "pwsh -File scripts/monitor.ps1",
    "docs:update": "node scripts/generate-analisis-sistema.js",
    "docs:check": "node scripts/check-analisis-sistema.js",
    "validate:encoding": "node scripts/validate-encoding.js",
    "validate:docs": "node scripts/validate-docs.js",
    "validate:html": "node scripts/validate-html.js",
    "validate:links": "node scripts/validate-links.js",
    "validate:public:critical": "node scripts/validate-public.js --build",
    "validate": "npm run check:format && npm run validate:encoding && npm run validate:docs && npm run validate:html && npm run validate:links && npm run validate:public:critical",
    "build:public": "node scripts/build-public.js",
    "validate:public": "npm run build:public && node scripts/validate-html.js --file public/index.html && node scripts/validate-links.js --root public",
    "watch:css": "echo 'CSS directo - no requiere compilación'",
    "build": "npm run format",
    "deploy": "git add . && git commit -m 'chore: actualizar sitio' && git push origin main",
    "version:patch": "npm version patch -m 'chore(release): v%s'",
    "version:minor": "npm version minor -m 'feat(release): v%s'",
    "version:major": "npm version major -m 'BREAKING CHANGE: v%s'",
    "preinstall": "echo '📦 Instalando dependencias de desarrollo...'",
    "postinstall": "echo '✅ Instalación completa. Ejecuta: npm run dev'"
  },
  "keywords": [
    "impresion-3d",
    "petg",
    "fabricacion-digital",
    "pachuca",
    "mexico"
  ],
  "author": "Mahitek 3D Lab",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "jsdom": "^24.1.0",
    "prettier": "^3.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%",
    "not dead",
    "not IE 11"
  ]
}
